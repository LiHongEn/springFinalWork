<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.li.GUI.Dao.IUserDao">
    <select id="queryAll" resultType="com.li.pojo.Student" >
        select * from jdbc.studentsimformation;
    </select>

    <delete id="delete" parameterType="int">
        delete from jdbc.studentsimformation where id =#{id};
    </delete>

    <insert id="add" parameterType="com.li.pojo.Student">
        insert into jdbc.studentsimformation (id,name,major,phone) values (#{id},#{name},#{major},#{phone});
    </insert>

    <select id="QueryById" parameterType="int" resultType="com.li.pojo.Student">
        select * from jdbc.studentsimformation where id = #{id};
    </select>

    <update id="update" parameterType="com.li.pojo.Student" >
        update jdbc.studentsimformation set name = #{name} ,major = #{major} ,phone = #{phone} where id = #{id};
    </update>
</mapper>